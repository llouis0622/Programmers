import java.util.*;

class Solution {
    public long solution(long n) {
        Double num = Math.sqrt(n);
        
        if(num == sqrt.intValue()) {
            return (long)Math.pow(num + 1, 2);
        } else return -1;
    }
}